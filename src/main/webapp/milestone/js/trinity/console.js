// Define console for Internet Explorer
if (!window.console) { 
	console = {
			log: function() {}
	};
}




function printScreen(text,timer)
{
	
	/*
if(LiveStreamObject.connect.continous.timer2)clearTimeout(LiveStreamObject.connect.continous.timer2);
if(timer != undefined)
	{
	LiveStreamObject.connect.continous.timer2=
		 setTimeout(function(){ 
				document.getElementById("livestatus").innerHTML =text;
		 }, 2000);
	}
else
	{
	document.getElementById("livestatus").innerHTML =text;
	}
	*/
}
